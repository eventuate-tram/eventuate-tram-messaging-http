subscription.message.1.subscriberId=1
subscription.message.1.channels=orders,order-history
subscription.message.1.callbackSubscriptionId=subscription1

subscription.message.2.subscriberId=2
subscription.message.2.channels=customers,customer-history
subscription.message.2.callbackSubscriptionId=subscription2

subscription.message.3.subscriberId=3
subscription.message.3.channels=test-channel
subscription.message.3.callbackSubscriptionId=subscription3


eventuate.rest.consumer.base.url=http://${DOCKER_HOST_IP:localhost}:8081/messages
eventuate.http.proxy.base.url=http://${DOCKER_HOST_IP:localhost}:8098/subscriptions

spring.datasource.url=jdbc:mysql://${DOCKER_HOST_IP:localhost}/eventuate
spring.datasource.username=mysqluser
spring.datasource.password=mysqlpw
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port=8081
